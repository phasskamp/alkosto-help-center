name: Debug Actions
on: 
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "GitHub Actions is working correctly! Triggered by push."
